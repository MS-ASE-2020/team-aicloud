events {
    worker_connections   2000;
}
http {
    server {
        listen 80;
        charset utf-8;

        location / {
            root /usr/share/nginx;
            proxy_pass http://localhost:9528;
        }
}
}
